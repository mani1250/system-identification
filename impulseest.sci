function X = impulseest(x,M,K,lambda,regul)
    regul = [];
    x = [in out];
    N = size(out,'*');
    if(K==0)
        K = zeros(1,1);
        out =  zeros()
        out(index) = impulsechannel(x(i,1),in(j,2),N,M,K(index),regul,lambda)
            end
        end
        X
    end
zz    
    
endfunction
